#coding:utf-8

def countnum(num,plusnum):
    i=0
    numbers=[]
    while i<num:
        print ("At the top i is %d" %i)
        numbers.append(i)
        i=i+plusnum
        print ("Numbers now:",numbers)
        print ("At the bottom i is %d"%i)

        print("The numbers:")

    for num in numbers:
        print num

num=raw_input("Your Number>\n")
plusnum=raw_input("\nYour Plus Number>\n")
countnum(int(num),int(plusnum))